# ML Speedrun — Learning Log

## Week 1, Day 1 — numpy + pandas fundamentals

### Topics covered
- numpy arrays: 1D vectors, 2D matrices, shape, dtype
- numpy broadcasting (stretching operations across dimensions without loops)
- axis=0 vs axis=1 (columns vs rows)
- Indexing/slicing, boolean masking (`arr[mask]`)
- Matrix math: element-wise (*) vs dot product (@), transpose
- pandas DataFrames: creation, .head(), .info(), .describe()
- Boolean filtering with & / | (and WHY and/or breaks on columns)
- groupby + .agg() — aggregating data by category
- merge — joining tables, how="left", handling NaN with fillna()

### Status: Core numpy/pandas — DONE
